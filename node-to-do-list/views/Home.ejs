<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="style.css" />
    <title><%= title %></title>
  </head>
  <body class="bg-dark">
    <div
      class="header container text-center bg-info text-light shadow-lg py-2 px-4 rounded mt-4"
    >
      <h1><%= date %></h1>
    </div>

    <div
      class="form-area container text-center bg-info text-light shadow-lg p-4 rounded mt-4"
    >
      <form action="/" method="post">
        <div class="form-group">
          <input
            type="text"
            class="form-control"
            name="title"
            placeholder="Task Name..."
          />
        </div>
        <div class="form-group">
          <select name="status" class="form-control">
            <option selected disabled>Status</option>
            <option value="pending">Pending</option>
            <option value="completed">Completed</option>
          </select>
        </div>
        <button class="btn btn-warning btn-block" type="submit">
          ADD TASK
        </button>
      </form>
    </div>

    <div
      class="main container text-center bg-info text-light shadow-lg p-4 rounded mt-4"
    >
      <% if(typeof data === typeof [] ){ %>
      <ul class="tasks_list">
        <% data.forEach(taskObj => { %>
        <li
          class="d-flex justify-content-between bg-light p-2 text-dark border-bottom"
        >
          <span class="task_name"> <%= taskObj.title %></span>
          <span class="task_status"><%= taskObj.status %></span>
          <button class="btn-sm btn btn-danger">X</button>
        </li>
        <% }) %>
      </ul>
      <% }else{%>
      <h2>No Tasks Found</h2>
      <% } %>
    </div>
  </body>
</html>
